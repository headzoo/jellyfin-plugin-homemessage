{
  "name": "jellyfin-plugin-home-message",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd Jellyfin.Plugin.HomeMessage && dotnet publish -c Release -f net8.0",
    "build:web": "node scripts/build.js",
    "build:prod": "node scripts/build.js --prod",
    "watch:web": "node scripts/build.js --watch",
    "watch:typecheck": "tsc -w --noEmit",
    "dev": "concurrently -k \"npm:watch:web\" \"npm:watch:typecheck\"",
    "cs:test": "cd Jellyfin.Plugin.HomeMessage && dotnet test",
    "cs:watch": "cd Jellyfin.Plugin.HomeMessage && dotnet watch build"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "esbuild": "^0.25.9",
    "esbuild-sass-plugin": "^3.3.1",
    "prettier": "^3.6.2",
    "sass": "^1.91.0",
    "typescript": "^5.9.2"
  }
}
